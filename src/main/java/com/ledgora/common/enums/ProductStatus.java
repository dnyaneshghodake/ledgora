package com.ledgora.common.enums;

/** CBS Product lifecycle status. Products start DRAFT, become ACTIVE on approval. */
public enum ProductStatus {
    DRAFT,
    ACTIVE,
    INACTIVE,
    RETIRED
}
