package com.ledgora.common.enums;

/**
 * CBS Product types for the Product engine. Maps to account categories but at the product
 * template level — an Account is opened under a Product.
 */
public enum ProductType {
    SAVINGS,
    CURRENT,
    FIXED_DEPOSIT,
    LOAN
}
