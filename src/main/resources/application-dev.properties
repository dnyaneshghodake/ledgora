# Ledgora DEV Profile — run with: mvn spring-boot:run -Dspring-boot.run.profiles=dev
#
# SECURITY: This profile enables development-only features.
# NEVER activate this profile in production.

# Enable H2 console for local development
spring.h2.console.enabled=true

# More verbose SQL logging
spring.jpa.show-sql=true
logging.level.com.ledgora=DEBUG
